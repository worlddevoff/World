/** Public brand — ticker stays $WORLD; X handle is the ownable home base. */
export const TOKEN_TICKER = '$WORLD';
export const X_HANDLE = 'buildworldhq';
export const X_URL = `https://x.com/${X_HANDLE}`;
export const X_HANDLE_AT = `@${X_HANDLE}`;

export const BRAND_TAGLINE = 'Buys build the map';
